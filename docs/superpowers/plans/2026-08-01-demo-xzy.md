# Demo XZY Portfolio Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a local, multi-route Chinese UI/UX portfolio skeleton named demo xzy.

**Architecture:** Use the existing vinext App Router starter. Keep portfolio content in a typed data module, share the site shell and case-study layout, and render three project routes plus AI and About routes. Use plain CSS tokens for the neutral wireframe direction and system color preferences.

**Tech Stack:** Next.js 16 App Router, React 19, vinext, TypeScript, CSS, Node test runner

## Global Constraints

- Project path is `C:\Users\大头男孩\Desktop\html ui\demo xzy`.
- Visible content is Chinese and uses structured sample copy without fabricated outcomes.
- Font stack starts with PingFang SC and falls back to Microsoft YaHei, Noto Sans SC, and sans-serif.
- Visual direction remains neutral and low fidelity; imagery is represented by labeled media slots.
- No database, authentication, form submission, analytics, deployment, or generated imagery.

---

### Task 1: Route acceptance tests

**Files:**
- Modify: `tests/rendered-html.test.mjs`

**Interfaces:**
- Consumes: built worker at `dist/server/index.js`
- Produces: route-level acceptance contract for `/`, `/work/consumer`, `/work/enterprise`, `/work/campaign`, `/ai`, and `/about`

- [ ] Write tests that fetch each route and assert status, landmark copy, navigation, and absence of starter metadata.
- [ ] Run `npm test` and confirm failure because the starter does not implement the portfolio.

### Task 2: Shared content and routes

**Files:**
- Create: `app/content.ts`, `app/components.tsx`, `app/work/[slug]/page.tsx`, `app/ai/page.tsx`, `app/about/page.tsx`
- Modify: `app/page.tsx`, `app/layout.tsx`

**Interfaces:**
- Produces: `Project`, `projects`, `getProject`, `SiteHeader`, `SiteFooter`, `MediaSlot`, and all public routes

- [ ] Add typed example content and shared semantic components.
- [ ] Replace the starter page with the portfolio homepage.
- [ ] Add the reusable case-study route and static params for three slugs.
- [ ] Add AI and About pages and site metadata.

### Task 3: Responsive wireframe styling and cleanup

**Files:**
- Modify: `app/globals.css`, `package.json`, `tests/rendered-html.test.mjs`
- Delete: `app/_sites-preview/SkeletonPreview.tsx`, `app/_sites-preview/preview.css`

**Interfaces:**
- Consumes: semantic class names from Task 2
- Produces: responsive neutral theme, system dark mode, focus states, and cross-platform scripts

- [ ] Implement desktop and mobile layout tokens, a consistent 16px radius system, and reduced-motion-safe interaction feedback.
- [ ] Remove the starter preview dependency and metadata.
- [ ] Make npm scripts work on Windows without Unix-only environment syntax.
- [ ] Run `npm test` and confirm all routes pass.
- [ ] Run `npm run lint` and fix real failures.

